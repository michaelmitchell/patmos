import { expect } from "chai";

//
import * as common from "../../src/lib/config";

//
describe("lib", () => {
  describe("scope", () => {
    //
    describe("scopify", () => {
      it("should return a function that will only operate on the supplied scope");
    });

    //
    describe("scopify_chainable", () => {
      it("it should return a chainable scoped function");
    });
  });
});
